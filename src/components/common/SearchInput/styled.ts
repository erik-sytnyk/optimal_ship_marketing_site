import styled from 'styled-components';

import {input as inputStyles} from 'src/styles/shared';

export const container = styled.div`
  display: flex;
  width: 30.9rem;
  position: relative;
`;

export const input = styled.input`
  ${inputStyles};
`;
